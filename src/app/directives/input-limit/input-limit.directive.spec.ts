import { InputLimitDirective } from './input-limit.directive';

describe('InputLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new InputLimitDirective();
    expect(directive).toBeTruthy();
  });
});
